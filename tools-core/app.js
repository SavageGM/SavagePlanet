const KEY='savagePlanetToolsCore.v0.1';const defaults={schema:'sp-core/0.1',settings:{campaignName:'Savage Planet',defaultTL:9,currency:'$'},tools:[{id:'sp-gcs-builder',name:'GCS Library Builder',icon:'GCS',description:'Build and maintain Savage Planet GCS equipment, traits, modifiers, and libraries.',state:'Existing'},{id:'sp-engineering',name:'Engineering Console',icon:'ENG',description:'Inventions, upgrades, repairs, analysis, parts, facilities, and project results.',state:'In Development'},{id:'sp-explosives',name:'Explosives Calculator',icon:'REF',description:'Explosive equivalence, charges, blast values, and Savage Planet explosive data.',state:'Planned'},{id:'sp-weather',name:'Weather & Environment',icon:'WX',description:'Weather, environment, travel conditions, and hazards.',state:'Existing'},{id:'sp-foundry',name:'Foundry Bridge',icon:'VTT',description:'Future export and exchange layer for Foundry VTT and GURPS Game Aid.',state:'Planned'}],projects:[],records:[],references:[]};let core=load();function load(){try{return {...structuredClone(defaults),...JSON.parse(localStorage.getItem(KEY)||'{}')}}catch{return structuredClone(defaults)}}function save(){localStorage.setItem(KEY,JSON.stringify(core));render()}function id(p){return p+'-'+Date.now().toString(36).toUpperCase()+'-'+Math.random().toString(36).slice(2,6).toUpperCase()}function esc(s=''){return String(s).replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]))}document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>{document.querySelectorAll('nav button').forEach(x=>x.classList.toggle('active',x===b));document.querySelectorAll('.view').forEach(v=>v.classList.toggle('active',v.id===b.dataset.view));document.getElementById('title').textContent=b.textContent});function tool(t){return `<article class="tool"><i>${esc(t.icon)}</i><h4>${esc(t.name)}</h4><p>${esc(t.description)}</p><footer><span>${esc(t.id)}</span><span>${esc(t.state)}</span></footer></article>`}function render(){const th=core.tools.map(tool).join('');dashTools.innerHTML=th;toolGrid.innerHTML=th;toolCount.textContent=core.tools.length;projectCount.textContent=core.projects.length;recordCount.textContent=core.records.length;projectTool.innerHTML=core.tools.map(t=>`<option value="${esc(t.id)}">${esc(t.name)}</option>`).join('');projectList.innerHTML=core.projects.length?core.projects.slice().reverse().map(p=>`<div class="row"><div><b>${esc(p.name)}</b><br><code>${esc(p.id)}</code></div><span>${esc(p.type)}</span><span>${esc(core.tools.find(t=>t.id===p.tool)?.name||p.tool)}</span><button data-pdel="${esc(p.id)}">Delete</button></div>`).join(''):'<div class="empty">No projects created.</div>';recent.innerHTML=core.projects.length?core.projects.slice(-4).reverse().map(p=>`<div class="row"><div><b>${esc(p.name)}</b><br><code>${esc(p.id)}</code></div><span>${esc(p.type)}</span><span>${new Date(p.created).toLocaleDateString()}</span><span></span></div>`).join(''):'<div class="empty">No projects yet.</div>';records();refList.innerHTML=core.references.length?core.references.map(r=>`<div class="row"><div><b>${esc(r.title)}</b><br><code>${esc(r.id)}</code></div><span>${esc(r.type)}</span><span></span><button data-rfdel="${esc(r.id)}">Delete</button></div>`).join(''):'<div class="empty">No references registered.</div>';campaign.value=core.settings.campaignName;tl.value=core.settings.defaultTL;currency.value=core.settings.currency;document.querySelectorAll('[data-pdel]').forEach(b=>b.onclick=()=>{core.projects=core.projects.filter(p=>p.id!==b.dataset.pdel);save()});document.querySelectorAll('[data-rfdel]').forEach(b=>b.onclick=()=>{core.references=core.references.filter(r=>r.id!==b.dataset.rfdel);save()})}function records(){let q=(search.value||'').toLowerCase(),f=filter.value;let a=core.records.filter(r=>(f==='all'||r.type===f)&&(!q||`${r.name} ${r.id} ${r.type}`.toLowerCase().includes(q)));recordList.innerHTML=a.length?a.map(r=>`<div class="row"><div><b>${esc(r.name)}</b><br><code>${esc(r.id)}</code></div><span>${esc(r.type)}</span><span>${r.tl?'TL '+esc(r.tl):'—'}</span><button data-del="${esc(r.id)}">Delete</button></div>`).join(''):'<div class="empty">No matching shared records.</div>';document.querySelectorAll('[data-del]').forEach(b=>b.onclick=()=>{core.records=core.records.filter(r=>r.id!==b.dataset.del);save()})}search.oninput=records;filter.onchange=records;function open(d){d.showModal()}newProject.onclick=()=>open(projectDialog);newProject2.onclick=()=>open(projectDialog);newRecord.onclick=()=>open(recordDialog);newRef.onclick=()=>open(refDialog);document.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>b.closest('dialog').close());projectForm.onsubmit=e=>{e.preventDefault();let d=Object.fromEntries(new FormData(projectForm));core.projects.push({id:id('SP-PROJ'),...d,created:new Date().toISOString()});projectDialog.close();projectForm.reset();save()};recordForm.onsubmit=e=>{e.preventDefault();let d=Object.fromEntries(new FormData(recordForm));core.records.push({id:id('SP-REC'),...d,created:new Date().toISOString(),customData:{}});recordDialog.close();recordForm.reset();save()};refForm.onsubmit=e=>{e.preventDefault();let d=Object.fromEntries(new FormData(refForm));if(core.references.some(r=>r.id===d.id))return alert('Reference ID already exists.');core.references.push({...d,created:new Date().toISOString()});refDialog.close();refForm.reset();save()};saveSettings.onclick=()=>{core.settings={campaignName:campaign.value||'Savage Planet',defaultTL:+tl.value||9,currency:currency.value||'$'};save()};reset.onclick=()=>{if(confirm('Reset all local Tools Core data?')){core=structuredClone(defaults);save()}};export.onclick=()=>{let blob=new Blob([JSON.stringify(core,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='savage-planet-tools-core.json';a.click();URL.revokeObjectURL(a.href)};import.onclick=()=>file.click();file.onchange=async()=>{try{let x=JSON.parse(await file.files[0].text());if(!x.schema?.startsWith('sp-core/'))throw Error('Not a Savage Planet Core file');core={...structuredClone(defaults),...x};save()}catch(e){alert('Import failed: '+e.message)}file.value=''};render();